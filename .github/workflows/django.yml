name: Django CI

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Build
      env:
        DB_SHM_SIZE: 1g
      run: |
        make build

    - name: Run tests
      run: make test-ci

    - name: Run linter
      run: make lint-check

    - name: Cleanup
      if: always()
      run: make clean
