#!/bin/bash

set -e

cd /app
python manage.py collectstatic --noinput  # Here or during build?
python manage.py migrate --noinput  # Here or during build?
gunicorn brasilio.wsgi:application --bind=0.0.0.0:5000 --workers=$WORKERS --log-file -
